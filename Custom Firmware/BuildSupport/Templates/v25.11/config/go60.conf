CONFIG_ZMK_HID_CONSUMER_REPORT_USAGES_FULL=y
CONFIG_SHINYGO60=y

# USB CDC requires the global interrupt-driven UART API. Keep the physical
# UART0 on the asynchronous API required by Go60's TRRS wired split.
CONFIG_UART_0_ASYNC=y
CONFIG_UART_0_INTERRUPT_DRIVEN=n
