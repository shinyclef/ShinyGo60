# Keymap parser fixtures

Place small, reviewed `.keymap` fixtures here when parser work begins. Fixtures must contain only the syntax needed by a test and must not include private layout
content copied into diagnostic output.
