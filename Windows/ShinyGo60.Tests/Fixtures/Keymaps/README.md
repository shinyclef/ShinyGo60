# Keymap parser fixtures

These small, reviewed exports cover layer reordering, harmless exporter formatting changes, and malformed binding counts. The project's full Go60 export supplies
the complex behavior and Unicode-path case; tests inspect its layer metadata without copying its private binding content into diagnostic output.
