# Protocol vectors

Versioned binary request and response vectors will live here once the message encoding is selected. The same vectors must pass through the USB and Bluetooth
transport implementations.
